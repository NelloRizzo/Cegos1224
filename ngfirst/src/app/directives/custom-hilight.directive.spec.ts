import { CustomHilightDirective } from './custom-hilight.directive';

describe('CustomHilightDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomHilightDirective();
    expect(directive).toBeTruthy();
  });
});
